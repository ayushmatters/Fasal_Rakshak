PORT=5000
MONGO_URI=mongodb://localhost:27017/fasal_rakshak
JWT_SECRET=fasal_rakshak_jwt_secret_dev_key_2025

# Email Configuration
# For development: set SMTP_HOST to use Ethereal or real SMTP
# Ethereal account (user provided) — dev only
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_USER=ransom.hudson83@ethereal.email
SMTP_PASS=uVMJK8arPpqVnwwCuB
SMTP_SECURE=false
EMAIL_FROM=no-reply@fasal-rakshak.local
OTP_EXPIRE_MINUTES=10

# Frontend URL for verification links (match Vite dev server)
FRONTEND_URL=http://localhost:5176

# Gmail SMTP (App Password) - configured per user request
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=mathpent05@gmail.com
SMTP_PASS=bacschufspyvnarr
SMTP_SECURE=false
